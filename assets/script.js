const Z={owner:"zavorynofficial",repo:"zavoryn-portfolio",branch:"main",email:"zavoryn@outlook.com",paths:{posts:"assets/img/post-designing",logos:"assets/img/brand-designing/logos",letterheads:"assets/img/brand-designing/letterheads",businessCards:"assets/img/brand-designing/business-cards",stationery:"assets/img/brand-designing/stationery",guidelines:"assets/img/brand-designing/brand-guidelines",social:"assets/img/social-media-management",ai:"assets/img/ai-automation"}};
const API=`https://api.github.com/repos/${Z.owner}/${Z.repo}/contents/`;const isImg=n=>/\.(jpg|jpeg|png|webp|gif|svg)$/i.test(n);async function contents(p){const r=await fetch(`${API}${p}?ref=${Z.branch}`,{headers:{Accept:"application/vnd.github+json"}});if(!r.ok)throw Error(r.status);return r.json()}
async function images(path){try{const items=await contents(path),out=[];for(const x of items){if(x.type==="file"&&isImg(x.name))out.push(x);else if(x.type==="dir"){try{const n=await contents(x.path);n.filter(y=>y.type==="file"&&isImg(y.name)).forEach(y=>out.push(y))}catch{}}}return out}catch(e){console.warn("Zavoryn catalogue",e);return[]}}
const clean=n=>n.replace(/\.[^/.]+$/,'').replace(/[-_]+/g,' ').replace(/\s+/g,' ').trim().replace(/\b\w/g,c=>c.toUpperCase());
function lightbox(src,alt){let b=document.getElementById('z-lightbox');b.querySelector('img').src=src;b.querySelector('img').alt=alt;b.classList.add('open')}
function mediaCard(f,label){const e=document.createElement('article');e.className='media-card reveal';e.innerHTML=`<button class="media-button" type="button"><div class="media-image"><img src="${f.download_url}" alt="${clean(f.name)}" loading="lazy"></div><div class="media-meta"><span>${label}</span><strong>${clean(f.name)}</strong></div></button>`;e.querySelector('button').onclick=()=>lightbox(f.download_url,clean(f.name));return e}
async function renderGallery(el,path,label,limit=999){if(!el)return;el.innerHTML='<div class="loading">Loading work…</div>';const fs=await images(path);if(!fs.length){el.innerHTML='<div class="notice">No work here yet. Upload images to this GitHub folder and commit to main.</div>';return}el.innerHTML='';fs.slice(0,limit).forEach(f=>el.appendChild(mediaCard(f,label)));observe()}
function observe(){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal:not(.visible)').forEach(x=>io.observe(x))}
document.addEventListener('DOMContentLoaded',()=>{const m=document.querySelector('.menu-btn'),n=document.querySelector('.nav-links');if(m)m.onclick=()=>n.classList.toggle('open');const b=document.getElementById('z-lightbox');if(b){b.onclick=e=>{if(e.target===b||e.target.classList.contains('lightbox-close'))b.classList.remove('open')};document.addEventListener('keydown',e=>{if(e.key==='Escape')b.classList.remove('open')})}observe()});
